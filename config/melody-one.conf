CONFIG_ZMK_HID_REPORT_TYPE_NKRO=y
CONFIG_ZMK_BLE=y
CONFIG_ZMK_BLE_EXPERIMENTAL_CONN=y
CONFIG_ZMK_BLE_PASSKEY_ENTRY=y
CONFIG_BT_MAX_CONN=5
CONFIG_BT_MAX_PAIRED=5
CONFIG_BT_DEVICE_NAME="Melody One"
CONFIG_BOARD_NICE_NANO=y
# Deep sleep re-enabled now that the kscan declares `wakeup-source;`, which
# arms the key matrix as a wake source so a keypress wakes the board again.
# Timeout is intentionally short (1 min) to make testing the wake fix quick —
# raise back to 720000 (12 min) for normal battery life once wake is confirmed.
CONFIG_ZMK_SLEEP=y
CONFIG_ZMK_IDLE_SLEEP_TIMEOUT=60000
CONFIG_ZMK_KSCAN_DEBOUNCE_PRESS_MS=5
CONFIG_ZMK_KSCAN_DEBOUNCE_RELEASE_MS=5

# Remove BLE slave latency (default 30) so the keyboard listens on every
# connection interval. Eliminates the wake-up lag after short typing pauses
# at the cost of slightly higher battery drain. macOS may ignore this value.
CONFIG_BT_PERIPHERAL_PREF_LATENCY=0
